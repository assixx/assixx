# Assixx CODEOWNERS
# Docs: https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-security/customizing-your-repository/about-code-owners
#
# Single team owns the whole repo. When the org grows (e.g. separate security
# or backend teams), add path-specific rules BELOW the catch-all — GitHub
# uses last-match-wins semantics, so later lines override earlier ones.
*   @assixx/core
