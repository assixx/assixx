# Docker Environment Configuration für Assixx
# Kopiere diese Datei nach .env und passe die Werte an

# Node Environment
NODE_ENV=production

# MySQL Database
MYSQL_ROOT_PASSWORD=StrongP@ssw0rd!123
MYSQL_DATABASE=assixx
MYSQL_USER=assixx_user
MYSQL_PASSWORD=AssixxP@ss2025!

# Application Secrets (WICHTIG: Ändern!)
JWT_SECRET=schneeseekleerehfeedrehzehwehtee
SESSION_SECRET=schneeseekleerehdrehzehwehtee

# SMTP Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@assixx.com

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Optional: Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379

# Optional: SSL/TLS (für Production)
SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
SSL_KEY_PATH=/etc/nginx/ssl/key.pem
