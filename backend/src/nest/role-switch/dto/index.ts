/**
 * Role Switch DTOs Barrel Export
 *
 * NOTE: Role switch endpoints use empty bodies for security.
 * All information comes from the authenticated JWT token.
 */

// No DTOs needed - all POST endpoints have empty bodies
// GET /status returns data directly from the JWT token
