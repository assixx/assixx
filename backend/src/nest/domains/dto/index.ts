/**
 * Domains DTO barrel export.
 *
 * Exports `AddDomainDto` (POST body) + `DomainIdParamDto` (UUID :id route
 * param). Consumed by `DomainsController` (Step 2.7).
 */
export * from './add-domain.dto.js';
export * from './verify-domain.dto.js';
