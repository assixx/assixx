{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Assixx Backend Test Configuration",

  "extends": "./tsconfig.json",

  "compilerOptions": {
    "types": ["node", "vitest/globals"],
    "noEmit": true
  },

  "include": ["src/**/*", "src/**/*.test.ts", "src/**/*.spec.ts"],

  "exclude": ["node_modules", "dist"]
}
