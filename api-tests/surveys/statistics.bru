meta {
  name: Get Survey Statistics (Admin)
  type: http
  seq: 5
}

get {
  url: {{base_url}}/surveys/{{survey_id}}/statistics
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
}

tests {
  test("should return 200 OK", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
