meta {
  name: Update Department (Admin)
  type: http
  seq: 4
}

put {
  url: {{base_url}}/departments/{{department_id}}
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Bruno Updated Dept {{$timestamp}}",
    "description": "Updated via Bruno API test"
  }
}

auth:bearer {
  token: {{auth_token}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
}

tests {
  test("should return 200 OK", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
