meta {
  name: Get Assignable Roles
  type: http
  seq: 3
}

get {
  url: {{base_url}}/roles/assignable
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
}

tests {
  test("should return 200 OK", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
